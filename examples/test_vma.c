#ifndef _TEST_VMA_C
#define _TEST_VMA_C

#include <kglob.h>
#include <mman.h>
#include <kprintf.h>

void test_vma()
{
	/*
	unsigned long *foo = vmalloc(2, 0);
	kprintf("END RESULT foo = %x\n", foo);
	*foo = 1000;
	kprintf("*foo = %d\n", *foo);
	foo = foo + (PAGE_SIZE / 4);
	*foo = 2000;
	kprintf("*foo = %d\n", *foo);
	*/
}

#endif
